# Mobile-Food-Truck-App
App using NativeWind CSS

my FoodTruckApp was created using Native react using expo and styled with NativeWind CSS. I used expo-router for the routing. My bigguest struggle with trying to install and run NativeWind in the application. I had to build this application several times because I could not get the app running.
